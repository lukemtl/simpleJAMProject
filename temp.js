// create basic HTML file with boilerplate, h1 / paragraph... link to stylesheet...

// README.md => # The most basic jamstack project

// GITHUB create repository, copy the remote add origin command from site

// TERMINAL: initialize the project as a git repo    (init)
   //        have the whole directory watched by git   ( add . )
   //        do first commit           ( commit -m "init commit")
   //        do remote add to set up link with git server (copy code from site)
   //        push to git server ( push -u origin master)